#include <stdio.h>

int main() {
    int rows, m, h;
    printf("Enter the number of rows: ");
    scanf("%d", &rows);
    for (m = 1; m <= rows; m++) {
        for (h = 1; h <= i; h++) {
            printf("* "); 
        }
        printf("\n");
    }

    return 0;
}


